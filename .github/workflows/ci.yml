# CI runs on Depot CI, not GitHub Actions.
# See .depot/workflows/ci.yml for the actual workflow.
# This stub exists to satisfy any branch protection rules that reference "CI".
name: CI (Depot)
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI runs on Depot CI — see https://depot.dev/orgs/p6gdqmvg63/ci"
