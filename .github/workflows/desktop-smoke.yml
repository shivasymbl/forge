# Desktop smoke build runs on Depot CI, not GitHub Actions.
# See .depot/workflows/ci.yml (desktop-smoke job) for the actual workflow.
name: Desktop Smoke (Depot)
on:
  workflow_dispatch:
jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Desktop smoke runs on Depot CI — see https://depot.dev/orgs/p6gdqmvg63/ci"
